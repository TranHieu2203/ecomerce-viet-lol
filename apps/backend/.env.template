STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# Story 9.2 — token xem trước CMS (HMAC); bắt buộc nếu dùng nút preview Admin (đồng bộ root .env.example)
CMS_PREVIEW_SECRET=change-me-cms-preview

# Epic 10 / FR-19 — danh sách id user Admin được publish banner (cách nhau bởi dấu phẩy). Để trống = mọi admin publish được.
CMS_PUBLISHER_ADMIN_IDS=

# ISR on-demand (Next.js) — khớp REVALIDATE_SECRET trong apps/backend-storefront/.env.local
# STOREFRONT_REVALIDATE_URL=http://localhost:8000
# REVALIDATE_SECRET=
