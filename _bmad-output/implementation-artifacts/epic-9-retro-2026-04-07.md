# Retrospective — Epic 9: CMS vận hành & nội dung tĩnh (Wave 3)

**Dự án:** ecomerce-viet-lol  
**Ngày:** 2026-04-07  
**Trạng thái epic:** `epic-9: done` (story 9.1–9.6 `done` theo `sprint-status.yaml`)

---

## Tóm tắt

| Story | Nội dung ngắn |
|-------|-----------------|
| 9.1 | Migration + revision snapshot |
| 9.2 | CRUD page, draft/publish, preview token |
| 9.3 | Admin editor SEO, footer, announcement, 404 copy |
| 9.4 | Media picker reuse |
| 9.5 | History & restore |
| 9.6 | Storefront `/p/[slug]`, metadata, AnnouncementBar, not-found CMS |

**Giá trị:** khóa FR-30/31/33/34/35/37/38 và UX J-Admin-6 cho nội dung tĩnh + ISR.

---

## Party mode — phiên rút gọn (theo dõi)

**Bob (Scrum Master):** Epic 9 đã đủ 6 story. Ta ghi nhận bài học và nợ kỹ thuật cho Wave 3, không chỉ “đánh dấu done”.

**Alice (PO):** Storefront cuối cùng đọc được trang CMS và copy 404/announcement từ cùng nguồn với Admin — đúng hứa với khách hàng vận hành.

**Winston (Architect):** ADR-12/13/14/16 đã có chỗ đứng trong code: tag `cms-pages` / `cms`, preview không cache công khai, revision không lộ store public.

**Amelia (Dev):** Hai lần fetch `getCmsSettingsPublic` trong một request được `cache()` gom lại — pattern nên giữ khi thêm widget CMS trên layout.

**Quinn (QA):** Nợ: integration HTTP publish/preview/404 vẫn defer từ 9.2; nên ưu tiên khi DB test ổn định.

**Barry (Quick flow):** Epic 10 (Growth) vẫn backlog — sprint **file** chưa “hết việc” toàn monorepo, nhưng **Wave 3 CMS** đã khép.

**Bob (Scrum Master):** Cam kết: mở sprint Growth = `create-story` / `sprint-planning` cho 10.1 khi team sẵn sàng.

---

## Điều làm tốt

- Chuỗi story 9.x men theo dependency rõ (migration → API → UI → media → history → SF).  
- Revalidate tag thống nhất với backend (`revalidate-storefront.ts`).  

## Cải thiện tiếp

- Bổ sung hoặc ổn định `integration:http` cho luồng CMS store.  
- Theo dõi upgrade SDK: xử lý 404 `FetchError` trên SF.  

## Việc tiếp theo

- Epic 10: bắt đầu từ `10-1-third-locale-settings-routing-fr-17` khi có story `ready-for-dev`.  
- Tùy chọn: `bmad-sprint-planning` refresh nếu thứ tự Growth đổi.
