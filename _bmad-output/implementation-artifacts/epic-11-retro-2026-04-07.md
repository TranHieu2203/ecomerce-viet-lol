# Retrospective — Epic 11: Wave 4 tin tức (FR-39…46, SC-15…16, NFR-10)

**Dự án:** ecomerce-viet-lol  
**Ngày:** 2026-04-07  
**Trạng thái epic:** `epic-11: done` (story 11.1–11.6 `done` theo `sprint-status.yaml`)

---

## Tóm tắt story

| Story | Nội dung ngắn |
|-------|----------------|
| 11.1 | Migration `store_cms_news_article`, revision `news_article` |
| 11.2 | Admin API `cms-news` — CRUD, sanitize, publish/unpublish |
| 11.3 | Store API list + detail + preview token (`article_id`) |
| 11.4 | Revalidate tag `cms-news` khi lưu / publish |
| 11.5 | Admin UI danh sách + editor (HTML vi/en; TipTap có thể bổ sung sau) |
| 11.6 | Storefront `/news`, `/news/[slug]`, nav seed + link nội bộ localized |

**Giá trị:** kênh tin tức đồng bộ với CMS hiện có (slug, SEO, draft/publish, ISR).

---

## Party mode — phiên rút gọn

**Bob (SM):** Epic 11 khép đủ 6 story; retro ghi bài học kỹ thuật và nợ có chủ đích.

**Charlie (Dev):** Preview token mở rộng `news_article` tương thích trang `page`; store `generateStaticParams` cần tolerant khi CI không bật Medusa — đã xử lý ở collections/categories.

**Dana (QA):** Unit test `cms-news` + cập nhật `cms-preview-token` / revision types; E2E tin tức khi pipeline DB + seed ổn định.

**Winston (Architect):** Khôi phục revision cho `news_article` chưa làm — có thể thêm sau nếu vận hành cần giống trang CMS.

**HieuTV (Lead):** Khép epic; roadmap TipTap hoặc block editor gắn với NFR paste/SC-15 khi ưu tiên UX.

---

## Điều làm tốt

- Tái sử dụng sanitize SEO/slug pattern từ CMS pages.  
- Một tag ISR `cms-news` rõ ràng cho on-demand revalidate.  
- Nav link `/news` đi `LocalizedClientLink` để khớp `[countryCode]`.

## Cải thiện / nợ

- Admin editor: nâng **TipTap** (hoặc block) theo ADR-19 khi có thời gian.  
- **Restore revision** cho bài tin (API + drawer) nếu cần parité với trang CMS.  
- Kiểm thử tích hợp Store API `cms-news` + seed bài mẫu tùy chọn.

## Việc tiếp theo (gợi ý)

- Chạy migration production + smoke test publish → storefront.  
- Cập nhật tài liệu vận hành: `CMS_PREVIEW_SECRET`, `article_id` trong preview token.
