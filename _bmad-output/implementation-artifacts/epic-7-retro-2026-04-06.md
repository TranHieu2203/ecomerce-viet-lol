# Retrospective — Epic 7: Seed dữ liệu Phụ lục A

**Dự án:** ecomerce-viet-lol  
**Ngày ghi nhận:** 2026-04-06  
**Ngôn ngữ tài liệu:** Tiếng Việt  
**Trạng thái epic (sprint-status):** Tất cả story 7.1–7.3 `done`

---

## 1. Tóm tắt epic

| Story | Tiêu đề (theo epics.md) | Sprint status |
|-------|-------------------------|---------------|
| 7.1 | Seed idempotent collections & products | done |
| 7.2 | Seed collection con ngân sách & metadata parent | done |
| 7.3 | Seed `metadata.i18n` tối thiểu (vi + en một phần) | done |

**Mục tiêu kế hoạch:** FR-15, FR-16; cây collection / product type / variant đúng Phụ lục A và PRD.

**Ghi chú nguồn dữ liệu phiên họp:** Trong `_bmad-output/implementation-artifacts/` **không** có file story chi tiết cho 7.1–7.3 (chỉ có `1-1-init-monorepo-medusa-backend.md`). Phần “deep story analysis” dựa trên định nghĩa epic trong `epics.md`, `project-context.md`, và trạng thái tracking — đây cũng là một **bài học quy trình** (mục 4).

---

## 2. Thành phần tham gia (vai trò hội thoại)

- **Bob (Scrum Master)** — điều phối, tâm lý an toàn, tổng hợp cam kết  
- **Alice (Product Owner)** — giá trị nghiệp vụ, FR, chấp nhận nghiệm thu  
- **Charlie (Senior Dev)** — seed, Medusa, idempotency, kiến trúc dữ liệu  
- **Dana (QA)** — kiểm chứng chạy seed lặp, locale, regression  
- **Elena (Junior Dev)** — onboarding, tài liệu chạy seed  
- **HieuTV-Team-MedusaV2 (Project Lead)** — quyết định ưu tiên và phạm vi tiếp theo  

---

## 3. Phiên họp rút gọn (định dạng party mode)

**Bob (Scrum Master):** Chào cả nhóm. Theo `sprint-status.yaml`, Epic 7 đã đủ story `done`. Hôm nay ta retro epic seed Phụ lục A — tập trung hệ thống và học hỏi, không đổ lỗi cá nhân.

**Alice (Product Owner):** Epic này khóa FR-15/16 và dữ liệu demo cho storefront; về mặt plan là mảnh cuối của chuỗi CMS + catalog + SF.

**Charlie (Senior Dev):** Điểm mạnh là seed **idempotent** — chạy lại không nổ unique là tiêu chí then chốt của 7.1; nếu làm đúng, team vận hành đỡ rất nhiều.

**Dana (QA Engineer):** Tôi quan tâm chạy **hai lần** seed trên DB gần production và đổi locale `en` để thấy fallback `vi` như kiến trúc — đúng acceptance 7.3.

**Elena (Junior Dev):** Em khó vì trong folder story không có file 7.x để đọc Dev Notes; phải lần vào `epics.md` và code seed. Mong lần sau có story file hoặc README seed rõ handles.

**Bob (Scrum Master):** Elena vừa chỉ ra một khoảng trống quy trình: **artifact story không đồng bộ với sprint**. Ta ghi vào action items.

**Alice (Product Owner):** `epics.md` có gợi ý E7 có thể xen sớm sau E1; thực tế triển khai đã xếp cuối roadmap 1–7. Cần xem lại lần sau: nếu SF cần dữ liệu sớm, thứ tự epic ảnh hưởng demo và test tích hợp.

**Charlie (Senior Dev):** Nợ kỹ thuật tiềm ẩn: seed phụ thuộc collection cha, metadata parent, `metadata.i18n` — bất kỳ thay đổi schema i18n ở epic 4 cần được phản chiếu trong script/README seed.

**Bob (Scrum Master):** Không có file `epic-6-retro-*.md` trong repo — đây là retro epic 7 **đầu tiên** trong chuỗi được lưu; không có “follow-through” từ retro epic 6 để đối chiếu.

**Dana (QA Engineer):** Epic tiếp theo: trong `epics.md` **không** có Epic 8. Roadmap BMad 1–7 coi như khép; bước tiếp theo là **mở scope mới** (PRD/epic mới) hoặc **vận hành/hardening**, không phải “Epic 8 theo file hiện tại”.

---

## 4. Điều làm tốt

- Hoàn thành đủ 3 story seed gắn Phụ lục A và FR-15/16 theo định nghĩa epic.  
- Nhấn mạnh **idempotency** và **i18n mẫu** (vi đầy đủ, en một phần + fallback) khớp `architecture.md` / `project-context.md`.  
- Epic 7 nằm cuối chuỗi nên tận dụng được module CMS, API, và storefront locale đã có từ epic trước.

---

## 5. Thách thức & cơ hội cải thiện

- **Thiếu story file cho 7.x:** Khó truy vết Dev Notes, review, và lessons trong retro tự động.  
- **Không có retro epic trước lưu file:** Mất chuỗi “action item từ epic N → thực thi epic N+1”.  
- **Phụ thuộc chéo:** Seed phụ thuộc cấu trúc catalog/i18n đã thiết kế ở epic 4; mọi drift spec cần cập nhật song song README hoặc script.

---

## 6. Rủi ro / nợ kỹ thuật (ghi nhận, không gán lỗi)

- Đồng bộ tài liệu **handles cố định** / minh chứng seed (epics yêu cầu “document trong README seed”).  
- Bổ sung hoặc duy trì **test tự động** (unit/integration) cho “chạy seed hai lần” nếu chưa có — giảm regression khi đổi Medusa hoặc metadata.  
- Làm rõ **ai xác nhận** “production-ready” sau epic 7 (deploy, UAT stakeholder) vì file sprint chỉ phản ánh story `done`, không thay acceptance vận hành.

---

## 7. Epic tiếp theo

**Không có Epic 8** trong `epics.md` hiện tại. Hướng đi gợi ý:

- **`bmad-correct-course`** hoặc **`bmad-create-prd` / chỉnh PRD** nếu có phạm vi mới (ví dụ checkout, tỉnh thành VN, v.v.).  
- **`bmad-sprint-planning`** khi có epic/story mới.  
- **`bmad-generate-project-context`** nếu codebase đổi nhanh sau khi mở scope.

---

## 8. Action items (SMART — không ước lượng thời gian tuyệt đối)

| # | Hành động | Owner gợi ý | Tiêu chí xong |
|---|-----------|-------------|----------------|
| A1 | Bổ sung hoặc cập nhật **file story** (hoặc checklist trong epic) cho các story đã `done` nhưng chưa có markdown trong `implementation-artifacts` | Scrum Master + Dev | Mỗi story 7.x có ít nhất link tới PR/commit + acceptance đã verify |
| A2 | Đảm bảo **README seed** (hoặc doc tương đương) liệt kê handles / bằng chứng i18n theo AC epic | Dev | PO có thể demo `vi`/`en` theo danh sách trong doc |
| A3 | Chạy **`bmad-sprint-status`** sau mỗi đợt thay đổi lớn để YAML khớp thực tế | Project Lead | `sprint-status.yaml` phản ánh đúng trạng thái |
| A4 | Quyết định có **chạy `bmad-qa-generate-e2e-tests`** cho luồng seed + storefront locale hay không | PO + QA | Quyết định có/không và mục kiểm thử được ghi lại |

---

## 9. Cam kết quy trình (team agreements)

1. Retro epic được **lưu file** và cập nhật `epic-*-retrospective` trong `sprint-status.yaml` khi kết thúc phiên.  
2. Story có artifact dev khi team dùng BMad story template — tránh “chỉ có status YAML”.  
3. Khi **không còn epic kế hoạch**, chuyển sang **Correct course / PRD** thay vì giả định Epic 8.

---

## 10. Đánh giá sẵn sàng (tóm tắt)

- **Theo story tracking:** Epic 7 coi như **hoàn thành theo định nghĩa sprint**.  
- **Theo vận hành:** Cần **HieuTV-Team-MedusaV2** xác nhận riêng: test lặp seed, deploy, và chấp nhận stakeholder — không suy diễn trong tài liệu này.

---

*Tài liệu sinh theo workflow `bmad-retrospective`; điều phối: Bob (Scrum Master).*
