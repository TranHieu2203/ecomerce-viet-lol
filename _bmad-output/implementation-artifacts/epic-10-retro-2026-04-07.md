# Retrospective — Epic 10: Growth (FR-17…21, NFR-6)

**Dự án:** ecomerce-viet-lol  
**Ngày:** 2026-04-07  
**Trạng thái epic:** `epic-10: done` (story 10.1–10.5 `done` theo `sprint-status.yaml`)

---

## Tóm tắt story

| Story | Nội dung ngắn |
|-------|----------------|
| 10.1 | Locale thứ ba (`ja`), routing + middleware đọc CMS, switcher theo `enabled_locales` |
| 10.2 | Banner draft/publish, Store chỉ published; bảng audit publication |
| 10.3 | RBAC publish qua `CMS_PUBLISHER_ADMIN_IDS` |
| 10.4 | Cửa sời hiển thị banner (`display_start_at` / `display_end_at`) |
| 10.5 | Campaign A/B, một campaign active, variant cố định theo visitor |

**Giá trị:** khóa bucket Growth trên PRD mà không phá Wave 2–3; mở rộng i18n và vòng đời nội dung banner có kiểm soát.

---

## Party mode — phiên rút gọn

**Bob (Scrum Master):** Epic 10 đủ 5 story; ta chốt bài học và việc còn lại trước khi co-track trong `sprint-status` là “khép”.

**Alice (PO):** Locale và publish banner là hai chỗ dễ gây hiểu nhầm với vận hành — env publisher và danh sách locale trong Admin cần được ghi trong hướng dẫn nội bộ.

**Charlie (Senior Dev):** Migration Growth gộp nhiều concern; may là đã tránh file generate Medusa có lệnh drop index. Luôn review diff migration trước khi merge.

**Dana (QA):** Ưu tiên thêm `integration:http` cho publish banner + store filter khi pipeline DB ổn; unit test `banner-store-public` là điểm khởi đầu tốt.

**Winston (Architect):** Middleware storefront gọi CMS: cần monitor lỗi khi backend tắt — fallback đã có nhưng cache memory có thể stale ngắn.

**HieuTV-Team-MedusaV2 (Project Lead):** Đồng ý khép epic; retrospective các epic trước (1–6, 8) được đánh **done** theo hồ sơ giao hàng đã ổn định, không chờ họp riêng từng epic.

**Bob (Scrum Master):** Ghi nhận. Hàng động: cập nhật doc vận hành (migrate, env, locale `ja`, campaign); cân nhắc UI chỉnh lịch banner đầy đủ thay vì chỉ PATCH API.

---

## Điều làm tốt

- Một migration có kiểm soát; model/module `store_cms` mở rộng nhất quán.  
- Tách rõ Store vs Admin (publish, RBAC, audit).  
- Storefront gắn `visitor_id` / banner A-B không phá cache tag `cms` hiện có.

## Cải thiện / nợ

- Form Admin chỉnh `display_*` trực quan (hiện đọc từ list).  
- Kiểm thử tích hợp luồng publish + store banner sau Growth.  
- Khi roadmap tiếp tục: `sprint-planning` / `correct-course` để thêm epic mới — không có Epic 11 trong `sprint-status` tại thời điểm này.

## Khép retrospective tùy chọn (Epic 1–6, 8)

Các mục `epic-*-retrospective` từng để **optional** khi epic đã **done** nhưng chưa ghi formal retro. Theo quyết định Project Lead trong phiên này, các epic **1, 2, 3, 4, 5, 6, 8** được coi là đã rút kinh nghiệm qua chuỗi story và review thực tế; trạng thái tracking chuyển **done** cùng đợt Epic 10 để sprint file phản ánh **khép hoàn toàn**.

---

## Việc tiếp theo (không ước lượng thời gian)

1. Cập nhật tài liệu vận hành / onboarding cho Growth (env + locale + campaign).  
2. Khi có epic mới trong PRD: chạy BMad `sprint-planning` rồi `create-story` cho story đầu backlog.  
3. Tùy chọn: `bmad-code-review` trên nhánh Epic 10 nếu chưa có vòng review độc lập.
